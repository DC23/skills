# Provenance

Installed from [`triage`](https://github.com/mattpocock/skills/tree/main/skills/engineering/triage) by Matt Pocock (MIT licence).

Changes from upstream:

- Added `roadmap` category role for maintainer-initiated planned work; descriptions on `bug` and `enhancement` clarified to distinguish community vs. maintainer origin
- `roadmap` wontfix path: comment and close only — does not write to `.out-of-scope/` (reserved for community enhancement rejections)
- Step 1 (Gather context): skip `.out-of-scope/` check for `roadmap` issues
- `OUT-OF-SCOPE.md`: guidance on optional ADR linkage when a rejection is grounded in an architectural decision
- `/setup-matt-pocock-skills` reference → `/setup-dc23-skills`
